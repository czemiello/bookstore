import React, { PropTypes } from 'react'
import BookListContainer from '../containers/book/list.jsx'
const ListBooksApp = React.createClass({
    render () {
        return (
            <BookListContainer />
        )
    }
})

export default ListBooksApp
