import React, { PropTypes } from 'react'
import Rejestracja from '../components/logowanie/rejestracja.js'
const RejestracjaApp = (props) => {
    return (
        <Rejestracja />
    )
}

export default RejestracjaApp
